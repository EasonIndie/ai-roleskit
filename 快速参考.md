# AI Character Toolkit 快速参考

## 🚀 快速开始

### 1. 激活环境
```bash
.\ai-toolkit-env\Scripts\activate
```

### 2. 配置API
```bash
python setup_keys.py
```

### 3. 运行演示
```bash
python demo.py
python character_usage_guide.py
```

---

## 🎭 角色创建

### CLI方式
```bash
# 专家角色
python cli.py character create expert --name "李博士"

# 用户角色
python cli.py character create user --name "小明"

# 组织角色
python cli.py character create organization --name "创新科技"
```

### 代码方式
```python
from ai_toolkit.models.schemas import Character, CharacterInfo, CharacterType

# 创建角色
character = Character(
    name="李博士",
    type=CharacterType.EXPERT,
    description="可持续发展专家",
    info=CharacterInfo(
        name="李博士",
        position="专家",
        experience="10年经验"
    )
)
```

---

## 💬 角色调用方法

### 方法1: 直接对话
```python
from ai_toolkit.ai.base import AIRequest

request = AIRequest(
    messages=[
        {"role": "system", "content": f"你是{character.name}，{character.description}"},
        {"role": "user", "content": "你的问题"}
    ]
)
response = await ai_provider.chat_completion(request)
```

### 方法2: 对话管理器
```python
# 创建会话
dialogue = await dialogue_manager.create_dialogue(character_id=character.id)

# 添加消息
await dialogue_manager.add_message(dialogue_id, dialogue.id, role="user", content="问题")

# 获取回复
response = await dialogue_manager.get_character_response(dialogue_id, character_id, "问题")
```

### 方法3: CLI对话
```bash
# 开始对话
python cli.py dialogue start <character_id>

# 发送消息
python cli.py dialogue message <character_id> "你的消息"
```

---

## 📁 文件结构

```
ai-agent/
├── src/ai_toolkit/          # 核心代码
├── config/                  # 配置文件
├── data/                    # 数据存储
├── ai-toolkit-env/         # 虚拟环境
├── .env                     # 环境变量
├── demo.py                 # 基础演示
├── character_usage_guide.py # 角色使用指南
├── test_setup.py           # 基础测试
└── setup_keys.py           # API配置
```

---

## 🔧 常用命令

| 功能 | 命令 |
|------|------|
| 查看帮助 | `python cli.py --help` |
| 创建角色 | `python cli.py character create <type> --name <name>` |
| 开始对话 | `python cli.py dialogue start <character_id>` |
| 创意探索 | `python cli.py explore start "想法"` |
| 运行分析 | `python cli.py analysis run <session_id>` |

---

## 🎯 角色类型说明

| 类型 | 用途 | 特点 |
|------|------|------|
| 专家 (EXPERT) | 专业咨询 | 深入、权威 |
| 用户 (USER) | 需求分析 | 实用、体验导向 |
| 组织 (ORGANIZATION) | 团队观点 | 客观、全面 |

---

## ⚠️ 故障排除

### Unicode错误
```python
# 避免使用Unicode符号
print("PASS: 成功")  # 而不是 print("✓ 成功")
```

### API密钥错误
```bash
# 重新配置
python setup_keys.py
```

### 导入错误
```python
# 确保路径正确
import sys
sys.path.insert(0, './src')
```

---

## 📞 获取帮助

- 详细文档: `使用说明书.md`
- 基础演示: `demo.py`
- 角色示例: `character_usage_guide.py`
- 测试工具: `test_setup.py`

---

**快速启动**: 激活环境 → 配置API → 运行demo → 创建角色 → 开始对话 🚀