# AI Character Toolkit 使用说明书

## 📋 目录
1. [系统概述](#系统概述)
2. [环境设置](#环境设置)
3. [配置API密钥](#配置api密钥)
4. [基本使用](#基本使用)
5. [角色创建与调用](#角色创建与调用)
6. [CLI命令详解](#cli命令详解)
7. [编程接口使用](#编程接口使用)
8. [高级功能](#高级功能)
9. [故障排除](#故障排除)
10. [示例代码](#示例代码)

---

## 🎯 系统概述

AI Character Toolkit 是一个动态AI角色生成工具包，支持：
- 创意探索和想法分析
- AI角色生成和管理
- 多角色对话系统
- 并发验证和集成分析
- 数据持久化存储

---

## 🚀 环境设置

### 1. 激活虚拟环境

```bash
# Windows
.\ai-toolkit-env\Scripts\activate

# 或使用PowerShell
powershell -Command "& .\ai-toolkit-env\Scripts\Activate.ps1"
```

### 2. 验证安装

```bash
# 运行基础测试
python test_setup.py

# 查看演示
python demo.py
```

### 3. 检查依赖

确保以下模块已安装：
- ✅ OpenAI (openai>=1.0.0)
- ✅ Anthropic (anthropic>=0.3.0)
- ✅ 异步支持 (aiohttp, aiofiles)
- ✅ CLI工具 (click, rich)
- ✅ 模板引擎 (jinja2)

---

## 🔑 配置API密钥

### 方法1：使用配置脚本

```bash
python setup_keys.py
```

按照提示选择AI提供商并输入API密钥：
- OpenAI: 从 https://platform.openai.com/api-keys 获取
- Claude: 从 https://console.anthropic.com/ 获取

### 方法2：手动配置

编辑 `.env` 文件：

```env
# AI Provider Configuration
AI_PROVIDER=openai
OPENAI_API_KEY=your_openai_api_key_here
CLAUDE_API_KEY=your_claude_api_key_here
```

### 配置文件结构

```
config/
├── default.yaml          # 默认配置
└── custom.yaml           # 自定义配置
```

---

## 🎭 角色创建与调用

### 角色类型

1. **专家角色 (EXPERT)**
   - 用途：提供专业知识和深度见解
   - 示例：科学家、顾问、行业专家

2. **用户角色 (USER)**
   - 用途：代表用户视角和需求
   - 示例：客户、用户、利益相关者

3. **组织角色 (ORGANIZATION)**
   - 用途：代表机构或群体观点
   - 示例：公司、部门、团队

### 创建角色的方法

#### 方法1：CLI方式

```bash
# 创建专家角色
python cli.py character create expert --name "李博士" --position "可持续发展专家"

# 创建用户角色
python cli.py character create user --name "小明" --position "产品经理"

# 创建组织角色
python cli.py character create organization --name "创新科技" --position "AI研发部门"
```

#### 方法2：代码方式

```python
import asyncio
from ai_toolkit.models.schemas import Character, CharacterInfo, CharacterType
from ai_toolkit.storage.file_storage import FileStorage

async def create_character():
    # 创建角色信息
    character_info = CharacterInfo(
        name="李博士",
        position="可持续发展专家",
        experience="15年环保和新能源领域经验"
    )

    # 创建角色
    character = Character(
        name="李博士",
        type=CharacterType.EXPERT,
        description="专注于可持续发展和绿色能源解决方案的专家",
        info=character_info
    )

    # 保存角色
    storage = FileStorage()
    await storage.save_character(character)

    return character
```

### 调用角色的方法

#### 1. 直接AI对话

```python
from demo import MockAIProvider
from ai_toolkit.ai.base import AIRequest

# 创建AI Provider
ai_provider = MockAIProvider({'api_key': 'your_api_key'})

# 使用角色进行对话
ai_request = AIRequest(
    messages=[
        {"role": "system", "content": f"你是{character.name}，{character.description}"},
        {"role": "user", "content": "请介绍一下您的专业领域"}
    ]
)

response = await ai_provider.chat_completion(ai_request)
print(f"{character.name}: {response.content}")
```

#### 2. 通过角色ID加载

```python
# 保存角色ID
character_id = "your-character-id"

# 在其他地方加载角色
storage = FileStorage()
character = await storage.load_character(character_id)
```

#### 3. 使用对话管理器

```python
from ai_toolkit.core.dialogue import DialogueManager
from ai_toolkit.core.character import CharacterManager

# 初始化管理器
character_manager = CharacterManager(ai_provider)
dialogue_manager = DialogueManager(ai_provider, character_manager)

# 创建对话会话
dialogue = await dialogue_manager.create_dialogue(
    character_id=character.id,
    title=f"与{character.name}的对话"
)

# 添加消息并获取回复
await dialogue_manager.add_message(
    dialogue_id=dialogue.id,
    role="user",
    content="您对AI发展有什么看法？"
)

response = await dialogue_manager.get_character_response(
    dialogue_id=dialogue.id,
    character_id=character.id,
    context="您对AI发展有什么看法？"
)
```

---

## 💻 CLI命令详解

### 基本命令

```bash
# 查看帮助
python cli.py --help

# 查看版本
python cli.py version
```

### 角色管理

```bash
# 创建角色
python cli.py character create <type> --name <name> [options]

# 列出角色
python cli.py character list

# 查看角色详情
python cli.py character show <character_id>

# 更新角色
python cli.py character update <character_id> [options]

# 删除角色
python cli.py character delete <character_id>
```

### 对话管理

```bash
# 开始对话
python cli.py dialogue start <character_id>

# 发送消息
python cli.py dialogue message <character_id> "你的消息"

# 查看对话历史
python cli.py dialogue list <character_id>

# 导出对话
python cli.py dialogue export <dialogue_id> --format json
```

### 创意探索

```bash
# 开始探索
python cli.py explore start "你的想法"

# 交互式探索
python cli.py explore start "你的想法" --interactive

# 查看探索历史
python cli.py explore list
```

### 分析工具

```bash
# 运行分析
python cli.py analysis run <session_id>

# 生成报告
python cli.py analysis report <session_id>

# 冲突检测
python cli.py analysis conflict <dialogue_id>
```

---

## 🔧 编程接口使用

### 基础使用流程

```python
import asyncio
from ai_toolkit.ai.openai_provider import OpenAIProvider
from ai_toolkit.core.character import CharacterManager
from ai_toolkit.core.dialogue import DialogueManager
from ai_toolkit.storage.file_storage import FileStorage

async def main():
    # 1. 初始化AI Provider
    ai_provider = OpenAIProvider({
        'api_key': 'your_api_key',
        'model': 'gpt-3.5-turbo'
    })
    await ai_provider.initialize()

    # 2. 初始化管理器
    character_manager = CharacterManager(ai_provider)
    dialogue_manager = DialogueManager(ai_provider, character_manager)

    # 3. 创建角色和对话
    # (具体代码见前面示例)
```

### 存储操作

```python
# 保存角色
await storage.save_character(character)

# 加载角色
character = await storage.load_character(character_id)

# 保存对话
await storage.save_dialogue(dialogue)

# 加载对话
dialogue = await storage.load_dialogue(dialogue_id)
```

### 并发验证

```python
from ai_toolkit.core.concurrent import ConcurrentValidator

validator = ConcurrentValidator(ai_provider)

# 并发验证多个角色
characters = [char1, char2, char3]
results = await validator.validate_concurrently(characters)

# 获取冲突报告
conflicts = await validator.detect_conflicts(characters)
```

---

## 🎨 高级功能

### 1. 创意探索引擎

```python
from ai_toolkit.core.exploration import CreativeExplorer

explorer = CreativeExplorer(ai_provider)

# 开始探索会话
session = await explorer.start_exploration("如何改进可再生能源系统？")

# 添加洞察
await explorer.add_insight(session.id, "AI可以优化能源分配")

# 生成探索报告
report = await explorer.generate_report(session.id)
```

### 2. 集成分析

```python
from ai_toolkit.core.analysis import IntegrationAnalyzer

analyzer = IntegrationAnalyzer(ai_provider)

# 分析多角色对话
analysis = await analyzer.analyze_dialogue(dialogue)

# 检测机会
opportunities = await analyzer.identify_opportunities(dialogue)

# 生成综合报告
report = await analyzer.generate_integration_report(analysis)
```

### 3. 模板定制

```python
from ai_toolkit.templates.prompts import template_manager

# 自定义角色模板
custom_template = """
你是{{name}}，{{description}}。
专业领域：{{expertise}}
经验背景：{{experience}}
"""

template_manager.add_template("custom_character", custom_template)
```

### 4. 流式对话

```python
# 使用流式响应
async for chunk in ai_provider.chat_completion_stream(request):
    print(chunk, end='', flush=True)
```

---

## 🔧 故障排除

### 常见问题

#### 1. Unicode编码错误

**问题**: `'gbk' codec can't encode character`

**解决**:
```python
# 使用UTF-8编码
import locale
locale.setlocale(locale.LC_ALL, 'en_US.UTF-8')

# 或避免使用Unicode字符
print("PASS: 操作成功")  # 而不是 print("✓ 操作成功")
```

#### 2. API密钥配置错误

**问题**: `Invalid API key`

**解决**:
```bash
# 重新配置API密钥
python setup_keys.py

# 或手动检查.env文件
cat .env
```

#### 3. 模块导入错误

**问题**: `ModuleNotFoundError`

**解决**:
```python
# 确保路径正确
import sys
sys.path.insert(0, './src')

# 检查虚拟环境
pip list | grep ai-toolkit
```

#### 4. 存储问题

**问题**: 角色加载失败

**解决**:
```python
# 检查存储目录
import os
print(os.listdir('./data'))

# 重新初始化存储
storage = FileStorage(base_path='./my_data')
```

### 调试技巧

1. **启用调试日志**:
```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

2. **使用测试脚本**:
```bash
python test_setup.py
```

3. **检查配置**:
```python
from ai_toolkit.utils.config import config
print(config)
```

---

## 📚 示例代码

### 完整的使用示例

```python
#!/usr/bin/env python3
"""
完整的角色创建和使用示例
"""

import asyncio
import sys
sys.path.insert(0, './src')

from ai_toolkit.models.schemas import Character, CharacterInfo, CharacterType
from ai_toolkit.storage.file_storage import FileStorage
from ai_toolkit.ai.openai_provider import OpenAIProvider
from ai_toolkit.core.character import CharacterManager
from ai_toolkit.core.dialogue import DialogueManager
from ai_toolkit.ai.base import AIRequest

async def complete_example():
    """完整示例"""
    print("=== AI角色完整使用示例 ===")

    # 1. 初始化AI Provider
    ai_provider = OpenAIProvider({
        'api_key': 'your_api_key',
        'model': 'gpt-3.5-turbo'
    })
    await ai_provider.initialize()

    # 2. 创建角色
    character_info = CharacterInfo(
        name="王教授",
        position="人工智能专家",
        experience="10年AI研究和应用经验"
    )

    character = Character(
        name="王教授",
        type=CharacterType.EXPERT,
        description="专门研究人工智能和机器学习的专家",
        info=character_info
    )

    # 3. 保存角色
    storage = FileStorage()
    await storage.save_character(character)
    print(f"✓ 角色已创建: {character.name} (ID: {character.id})")

    # 4. 初始化管理器
    character_manager = CharacterManager(ai_provider)
    dialogue_manager = DialogueManager(ai_provider, character_manager)

    # 5. 创建对话会话
    dialogue = await dialogue_manager.create_dialogue(
        character_id=character.id,
        title=f"与{character.name}的专业对话"
    )

    # 6. 进行对话
    questions = [
        "请介绍一下AI在医疗领域的最新应用",
        "您认为AI发展的最大挑战是什么？",
        "对于想学习AI的初学者，您有什么建议？"
    ]

    for i, question in enumerate(questions, 1):
        print(f"\n{i}. 问题: {question}")

        # 添加用户消息
        await dialogue_manager.add_message(
            dialogue_id=dialogue.id,
            role="user",
            content=question
        )

        # 获取角色回复
        response = await dialogue_manager.get_character_response(
            dialogue_id=dialogue.id,
            character_id=character.id,
            context=question
        )

        print(f"{character.name}: {response}")

    # 7. 显示对话总结
    print(f"\n=== 对话总结 ===")
    messages = await dialogue_manager.get_dialogue_messages(dialogue.id)
    print(f"对话轮次: {len([m for m in messages if m.role == 'user'])}")
    print(f"角色ID: {character.id}")
    print(f"对话ID: {dialogue.id}")

if __name__ == "__main__":
    asyncio.run(complete_example())
```

### 快速测试脚本

```python
#!/usr/bin/env python3
"""
快速功能测试
"""

import asyncio
from character_usage_guide import create_character_examples, demonstrate_character_usage

async def quick_test():
    """快速测试所有功能"""
    print("=== 快速功能测试 ===")

    # 创建测试角色
    characters = create_character_examples()
    print(f"✓ 创建了 {len(characters)} 个测试角色")

    # 测试每个角色
    for character in characters:
        await demonstrate_character_usage(character)

    print("\n✓ 所有功能测试完成！")

if __name__ == "__main__":
    asyncio.run(quick_test())
```

---

## 📖 参考资料

### 配置文件示例

```yaml
# config/default.yaml
ai:
  provider: "openai"
  timeout: 30
  max_retries: 3

openai:
  model: "gpt-4"
  api_key: "${OPENAI_API_KEY}"
  max_tokens: 2000
  temperature: 0.7

storage:
  type: "file"
  file:
    base_path: "./data"
    format: "json"
```

### API参考

主要类和方法：
- `Character`: 角色数据模型
- `CharacterManager`: 角色管理器
- `DialogueManager`: 对话管理器
- `CreativeExplorer`: 创意探索引擎
- `FileStorage`: 文件存储系统

---

## 🎉 总结

现在你已经掌握了AI Character Toolkit的完整使用方法：

1. **环境设置** ✓ - 虚拟环境和依赖配置
2. **API配置** ✓ - OpenAI/Claude API密钥设置
3. **角色创建** ✓ - 三种类型的角色创建方法
4. **角色调用** ✓ - 多种调用和使用方式
5. **对话管理** ✓ - 完整的对话系统
6. **高级功能** ✓ - 探索、分析、验证等

开始创建你的AI角色，享受智能对话的乐趣吧！

---

**最后更新**: 2025年11月6日
**版本**: v1.0.0
**作者**: AI Character Toolkit Team